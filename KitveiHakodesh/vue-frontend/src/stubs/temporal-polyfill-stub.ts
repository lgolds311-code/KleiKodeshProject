// Chromium 138+ (WebView2 1.0.3912 = Chromium 147) ships Temporal natively.
// This stub replaces the temporal-polyfill/global import from @hebcal/core
// so the 118KB polyfill is excluded from the bundle.
export {}
