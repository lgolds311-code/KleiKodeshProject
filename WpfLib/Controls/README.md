# Controls

Custom WPF controls for common UI patterns.

## Components

- `UpDownTextBox` — TextBox with up/down spinner controls for numeric input
- `UpDownFloatTextBox` — TextBox with spinner controls for floating-point numbers
