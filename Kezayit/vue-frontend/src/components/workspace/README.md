# workspace

Workspace management UI.

**WorkspaceManagerPage.vue** — lists workspaces with create, rename, and delete actions. Switching workspaces reloads all tabs from the selected workspace's IDB keys. All workspace state logic lives in `workspaceStore`, not here.
