/**
 * Shared types for the book-view feature.
 * Import from here — never from a component file.
 */

export type SearchMode = 'content' | 'commentary'
export type SidePanelMode = 'toc' | 'commentary-filter'
